---
import Layout from '../layouts/Layout.astro';
import { getCurrentUser } from '../lib/auth';

const user = await getCurrentUser(Astro.cookies);
const error = Astro.url.searchParams.get('error');
---

<Layout title="Hive Reader - Save Articles to Your Kindle">
  <div slot="nav">
    {user ? (
      <a href="/dashboard" class="btn btn-primary">Dashboard</a>
    ) : (
      <a href="/api/auth/login" class="btn btn-primary">Sign In with Google</a>
    )}
  </div>

  <div class="hero">
    <div class="container">
      {error && (
        <div class="alert alert-error">
          {error === 'invalid_state' && 'Invalid authentication state. Please try again.'}
          {error === 'authentication_failed' && 'Authentication failed. Please try again.'}
        </div>
      )}

      <h1>🐝 Collect Knowledge Like a Hive</h1>
      <p class="subtitle">
        Save articles from anywhere on the web to your Kindle. Build your personal knowledge hive, one article at a time.
      </p>

      <div class="features">
        <div class="feature-card">
          <div class="feature-icon">🌐</div>
          <h3>Browser Extension</h3>
          <p>Install our Chrome/Edge extension and send articles with one click</p>
        </div>

        <div class="feature-card">
          <div class="feature-icon">📖</div>
          <h3>EPUB Format</h3>
          <p>Articles are converted to beautiful EPUB files optimized for reading</p>
        </div>

        <div class="feature-card">
          <div class="feature-icon">📧</div>
          <h3>Email to Kindle</h3>
          <p>Automatically send articles to your Kindle email address</p>
        </div>
      </div>

      <div class="cta">
        {user ? (
          <a href="/dashboard" class="btn btn-primary btn-large">Go to Dashboard</a>
        ) : (
          <a href="/api/auth/login" class="btn btn-primary btn-large">Get Started Free</a>
        )}
      </div>
    </div>
  </div>
</Layout>

<style>
  .hero {
    padding: 4rem 0;
    text-align: center;
  }

  h1 {
    font-size: 3rem;
    margin-bottom: 1rem;
    color: white;
    text-shadow: 0 2px 10px rgba(0, 0, 0, 0.2);
  }

  .subtitle {
    font-size: 1.25rem;
    color: rgba(255, 255, 255, 0.95);
    margin-bottom: 3rem;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }

  .features {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 2rem;
    margin-bottom: 3rem;
  }

  .feature-card {
    background: white;
    padding: 2rem;
    border-radius: 12px;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
    transition: transform 0.2s;
  }

  .feature-card:hover {
    transform: translateY(-5px);
  }

  .feature-icon {
    font-size: 3rem;
    margin-bottom: 1rem;
  }

  .feature-card h3 {
    margin-bottom: 0.5rem;
    color: #FDB813;
    font-weight: 600;
  }

  .feature-card p {
    color: #666;
    line-height: 1.6;
  }

  .cta {
    margin-top: 2rem;
  }

  .btn-large {
    font-size: 1.25rem;
    padding: 1rem 2.5rem;
  }
</style>
